//


#include <bits/stdc++.h>
using namespace std;

int main() {

int n;
cin>>n;

int sum=0;
int count = log10(n)+1;
int temp=n;
while(temp!=0){
    int rem = temp%10;
    sum+=pow(rem,count);
    temp=temp/10;
    
}
if(sum==n) cout<<"true";
else cout<<"False";
}
