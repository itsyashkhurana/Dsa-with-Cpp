#include<bits/stdc++.h>
using namespace std;
int main(){
    unordered_multiset<int> s; // duplicate
    s.insert(10);
    s.insert(20);
    s.insert(30);
    s.insert(10);
    s.insert(40);
    s.insert(30);
    
    
    for(auto it =s.begin();it!=s.end();it++){
     cout<<*(it)<<" ";
} 
cout<<"\n"; 
}
